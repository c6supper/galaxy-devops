# Galaxy Benchmark Script

## Installation
**Centos**
```
sudo yum install sysbench bc -y
```
**Ubuntu**
```
sudo apt-get install sysbench bc -y
```

### Run
```
./benchmark.sh > benchmark.$(date +"%Y_%m_%d_%I_%M_%p")
```